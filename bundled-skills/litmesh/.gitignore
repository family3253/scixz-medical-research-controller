# 依赖（安装时由 npm install 生成，体积大，不入库）
node_modules/

# 日志 / 临时
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# 本地密钥 / 本地配置（不要提交）
.env
.env.local
config.local.json
credentials.yaml

# 编辑器 / OS
.idea/
.vscode/
*.swp
